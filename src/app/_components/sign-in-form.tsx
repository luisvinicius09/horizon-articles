export default function SignInForm() {
  return (
    <div>
      <form>
        <fieldset>
          <div>
            <label htmlFor="email">Email</label>
            <input type="email" id="email" />
          </div>
        </fieldset>

        <fieldset>
          <div>
            <label htmlFor="email">Email</label>
            <input type="email" id="email" />
          </div>
        </fieldset>

        <button type="submit">
          <p>Sign In</p>
        </button>
      </form>
    </div>
  );
}
